# Database Configuration
POSTGRES_DB=healthstash
POSTGRES_USER=healthstash
POSTGRES_PASSWORD=changeme-strong-password

# TimescaleDB Configuration
TIMESCALE_DB=healthstash_vitals
TIMESCALE_USER=healthstash
TIMESCALE_PASSWORD=changeme-strong-password

# Redis Configuration
REDIS_PASSWORD=changeme-strong-password

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme-strong-password
MINIO_BROWSER=off

# Application Security
SECRET_KEY=your-secret-key-change-this-to-random-string
ENCRYPTION_KEY=your-32-byte-encryption-key-change-this
JWT_SECRET_KEY=your-jwt-secret-change-this-to-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application Settings
MAX_UPLOAD_SIZE_MB=500
CORS_ORIGINS=http://localhost,https://localhost

# Frontend Configuration
VITE_API_URL=http://localhost:8000

# Port Configuration
HTTP_PORT=80
HTTPS_PORT=443

# Backup Configuration
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30

# Session Configuration
SESSION_TIMEOUT_MINUTES=30
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION_MINUTES=15

# Storage Quotas (in MB)
DEFAULT_USER_QUOTA_MB=5000
MAX_FILE_SIZE_MB=500

# Password Policy
MIN_PASSWORD_LENGTH=12
REQUIRE_UPPERCASE=true
REQUIRE_LOWERCASE=true
REQUIRE_NUMBERS=true
REQUIRE_SPECIAL_CHARS=true